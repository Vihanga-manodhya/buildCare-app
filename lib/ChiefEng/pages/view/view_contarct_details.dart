import 'package:flutter/material.dart';

class ViewContarctPage extends StatelessWidget {
  const ViewContarctPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("View Contact Plan")),
      body: SingleChildScrollView(child: Column(children: [])),
    );
  }
}
