import 'package:flutter/material.dart';

class PrincipalSignupPage extends StatelessWidget {
  const PrincipalSignupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Principal Signup Page")),
    body: Column(children: [
     
    ],),);
  }
}
