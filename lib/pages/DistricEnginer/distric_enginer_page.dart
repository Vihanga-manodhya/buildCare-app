import 'package:flutter/material.dart';

class DistricSignupPage extends StatelessWidget {
  const DistricSignupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("distric Signup Page")));
  }
}
