import 'package:flutter/material.dart';

class ToSignupPage extends StatelessWidget {
  const ToSignupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("To Signup Page")));
  }
}
